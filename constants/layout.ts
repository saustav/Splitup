/** Shared screen layout constants for consistent spacing across the app. */
export const layout = {
  screenPadding: 16,
  tabScrollBottom: 88,
  cardRadius: 14,
  heroRadius: 20,
  contentMaxWidth: 480,
  headerIconSize: 34,
} as const;
